# SKEIN — Structural Knowledge and Enterprise Intelligence Network
# Python dependencies
# ================================================
# Priority: install only what you need.
#
# Core (always required):
#   pip install pyyaml requests
#
# Full production stack:
#   pip install -r requirements.txt
#
# Platform adapters:
#   Databricks: pip install databricks-sdk mlflow pyspark
#   Fabric:     pip install azure-identity azure-storage-file-datalake
#
# Testing:
#   python3 -m unittest discover -s tests -p "test_*.py"

# ── Core (always required) ─────────────────────────────────────────────────────
pyyaml>=6.0                    # YAML config file parsing
requests>=2.31.0               # HTTP client for Ollama provider

# ── LLM Providers — install the provider you use ──────────────────────────────
# Default: Ollama (local, no API key, free)
# No extra package needed for Ollama — uses urllib.request

# Anthropic Claude
# anthropic>=0.40.0

# OpenAI / Azure OpenAI
# openai>=1.30.0

# ── Reasoning Framework Integrations (optional) ───────────────────────────────
# LangChain LCEL strategy:
# langchain>=0.3.0
# langchain-core>=0.3.0
# langchain-anthropic>=0.3.0
# langchain-openai>=0.2.0
# langchain-community>=0.3.0

# LangGraph multi-step strategy:
# langgraph>=0.2.0

# CrewAI crew delegation:
# crewai>=0.80.0

# ── Observability (optional but recommended) ──────────────────────────────────
# Prometheus metrics export:
# prometheus-client>=0.20.0

# ── Platform Adapters (optional) ──────────────────────────────────────────────
# Databricks:
# databricks-sdk>=0.40.0
# mlflow>=2.14.0
# pyspark>=3.5.0

# Microsoft Fabric / Azure:
# azure-identity>=1.17.0
# azure-storage-file-datalake>=12.14.0

# ── Testing ────────────────────────────────────────────────────────────────────
# Run without pytest:
#   python3 -m unittest discover -s tests -p "test_*.py" -v
#
# Run with pytest (if available):
#   pytest tests/ -v --tb=short
#
# Optional enhanced testing:
# pytest>=8.0.0
# pytest-cov>=5.0.0
# pytest-timeout>=2.3.0   # enforce test timeouts

# ── Development ────────────────────────────────────────────────────────────────
# mypy>=1.8.0             # type checking
# ruff>=0.3.0             # linting + formatting
